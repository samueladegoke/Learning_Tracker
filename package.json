{
    "private": true,
    "scripts": {
        "dev": "start cmd /c \"cd frontend && npm run dev\" && cd backend && uvicorn app.main:app --reload --port 8000",
        "dev:win": "start cmd /c \"cd frontend && npm run dev\" && cd backend && uvicorn app.main:app --reload --port 8000",
        "dev:unix": "cd frontend && npm run dev & cd backend && uvicorn app.main:app --reload --port 8000",
        "dev:frontend": "cd frontend && npm run dev",
        "dev:backend": "cd backend && uvicorn app.main:app --reload --port 8000",
        "convex": "npx convex dev",
        "test:convex": "npx convex-test",
        "build": "cd frontend && npm install && npm run build"
    },
    "dependencies": {
        "convex": "^1.31.4",
        "dotenv": "^17.2.3"
    },
    "devDependencies": {
        "@edge-runtime/vm": "^5.0.0",
        "convex-test": "^0.0.41",
        "vitest": "^3.1.1"
    }
}
